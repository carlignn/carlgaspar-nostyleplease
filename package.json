{
  "name": "carlgaspar-nostyleplease",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tinacms dev -c \"hugo server --cleanDestinationDir\"",
    "build": "tinacms build && hugo --cleanDestinationDir",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tinacms/cli": "^1.12.0",
    "tinacms": "^1.5.13"
  }
}
